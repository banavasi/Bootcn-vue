// Re-export from the @bootcn-vue/buttons package
// This allows the playground to use the package version while maintaining local import paths
export { Button, buttonVariants, type ButtonVariants } from "@bootcn-vue/buttons";
